<div class="homepage">
  <img
    src="{{ 'weeklyhow-logo.png' | asset_img_url: 'large' }}"
    width="{{ image.width }}"
    height="{{ image.height }}"
    class="logo"
    loading="eager">
  <h1>Welcome to Shopify Theme Development course!</h1>
{% comment %} <p>{% render 'homepage-text' %}</p> {% endcomment %}
</div>